pymupdf>=1.24.0
pydantic>=2.6.0
tqdm>=4.66.0
yake>=0.4.8
textstat>=0.7.3
pytesseract>=0.3.10
pillow>=10.0.0
torch>=2.2.0
transformers>=4.45.0
accelerate>=0.30.0
bitsandbytes>=0.43.0 ; platform_system != "Darwin"

# storage/ (OneDrive persistent-storage backend) -- now wired into
# modules/json_writer.py, so these are required for normal pipeline runs,
# not just standalone storage/ usage. See storage/requirements-storage.txt.
msal>=1.28.0
requests>=2.31.0
PyYAML>=6.0
