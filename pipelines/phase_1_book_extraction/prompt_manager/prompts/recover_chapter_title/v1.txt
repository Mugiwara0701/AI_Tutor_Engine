You are recovering a chapter's title for an NCERT textbook. Deterministic
(font-size based) extraction failed because the chapter's opening page is
set (in whole or in part) in a legacy, non-Unicode Hindi/Sanskrit typing
font (e.g. Walkman-Chanakya-family fonts) -- the PDF's text layer decodes
to meaningless Latin-looking glyph-code noise for these lines, not the
real printed text, even though the lines render correctly as printed
Devanagari on the page.

Book this chapter belongs to (weak hint only, may be wrong -- do not let
it override what the image actually shows): {{ book_title }}
Subject (weak hint only, may be wrong): {{ subject }}

Deterministic candidate title(s) pulled from the largest-font line(s) on
the opening page (this is the SAME script-mismatched text layer described
above -- likely glyph-code noise, not real text; use only as a weak
positional hint for WHERE the title sits on the page, never as the answer
itself): {{ candidate_titles }}

Best-effort Tesseract OCR of the opening page(s) (may be empty, partial,
or wrong -- use only as a weak hint, never trust it over the image):
{{ ocr_text_first_pages }}

Look at the attached page image and identify the chapter's real title --
usually the largest/most prominent text block on the opening page, often
set apart from the body text by size, decoration, or a chapter-number
marking next to it. Do not confuse a chapter-number graphic (a bare
numeral or "अध्याय <n>" / "पाठ <n>" marking), a running header/footer, or
a pull-quote/caption for the title itself.

Source textbook language: {{ target_language_name }}. The recovered title
must be written in {{ target_language_name }} exactly as printed on the
page -- do NOT translate into English unless {{ target_language_name }}
is English.

Transcribe only what is visually supported by the image -- never invent a
title unrelated to what's actually printed, and never fall back to one of
the candidate/OCR hints above if it does not match what the image shows.
Respond with a single valid JSON object and nothing else, with exactly
this one key:
chapter_title: {"value": <string>, "confidence": <0-1 float>, "evidence_basis": <short string, e.g. "large decorative text at top of opening page">}
